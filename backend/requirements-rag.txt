# ============================================================
# KrisiSar AI - OPTIONAL RAG dependencies
# Only needed for the vector-search version of the
# Government Scheme Agent. These are heavy (sentence-transformers
# pulls in PyTorch, ~2.5GB) so they are kept out of the core install.
#
# Install AFTER the core deps:
#     pip install -r requirements.txt
#     pip install -r requirements-rag.txt
# ============================================================

chromadb>=0.5.23
sentence-transformers>=3.3.1
